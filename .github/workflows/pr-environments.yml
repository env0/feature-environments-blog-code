name: "PR Environments"
on:
  pull_request:
    types:
      - created
      - closed
      - synchronize
jobs:
  create_environment:
    name: "Create Environment"
    runs-on: node:lts
    steps:
      - name: deploy
        run: ./scripts/deploy-env.sh
